# پروژه پایانی مکتب ۱۳۵ - سامانه خدمات منزل 

این پروژه یک وب‌سایت برای ارائه خدمات منزل است که در آن مشتریان درخواست ثبت کرده و کارشناسان پیشنهاد ارائه می‌دهند.

## 🏗 معماری و ابزارهای پروژه
* **معماری:** Onion Architecture (معماری پیازی)
* **دیتابیس:** Entity Framework Core با پیکربندی Fluent API
* **احراز هویت:** Microsoft Identity
* **لاگ:** استفاده از Seq برای ذخیره‌سازی لاگ‌ها
* **رابط کاربری:** Bootstrap و Razor Pages/MVC

---

## ✅ وضعیت فاز اول (تکمیل شده)
* تحلیل و طراحی موجودیت‌ها و روابط آن‌ها
* لایه‌بندی پروژه طبق ساختار Onion
* پیاده‌سازی عملیات CRU در لایه Repository برای سرویس‌ها و دسته‌بندی‌ها
* افزودن Seed Data اولیه به سیستم

## 🛠 وضعیت فاز دوم (در حال اجرا)
تا این لحظه موارد زیر پیاده‌سازی شده‌اند:
* **Logging:** پیاده‌سازی لاگ در تمامی لایه‌ها و اتصال به Seq
* **Identity:** افزودن قابلیت Login و Register با استفاده از Microsoft Identity
* **Areas:** تفکیک دسترسی‌ها با استفاده از Area برای هر نقش
* **Customer Profile:** پیاده‌سازی بخش نمایش و ویرایش پروفایل مشتری

---

## 📌 الزامات فنی رعایت شده
* **حذف نرم (Soft Delete):** حذف داده‌ها در دیتابیس به صورت منطقی انجام می‌شود.
* **تاریخ شمسی:** تمامی نمایش‌ها و ورودی‌های تاریخ به صورت شمسی (Persian Date) است.
* **مدیریت تصاویر:** تصاویر روی دیسک ذخیره شده و فقط آدرس آن‌ها در دیتابیس قرار می‌گیرد.
* **عدم استفاده از Generic Repository:** طبق قوانین پروژه از این الگو استفاده نشده است.