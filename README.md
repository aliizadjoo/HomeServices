# پروژه پایانی مکتب ۱۳۵ - سامانه خدمات منزل 🏠

این پروژه یک وب‌سایت برای ارائه خدمات منزل است که در آن مشتریان می‌توانند سفارش ثبت کرده و کارشناسان برای انجام خدمات پیشنهاد ارائه دهند.  
سیستم دارای نقش‌های مختلف (Admin، Customer، Expert) بوده و بر اساس معماری Onion پیاده‌سازی شده است.

---

## 🏗 معماری و ابزارهای پروژه

- **معماری:** Onion Architecture  
- **Backend:** ASP.NET Core MVC  
- **ORM:** Entity Framework Core (Fluent API)  
- **Database:** SQL Server  
- **Authentication & Authorization:** Microsoft Identity  
- **Logging:** Serilog + Seq  
- **UI:** Razor Pages / MVC + Bootstrap  
- **Persian Date:** تبدیل و نمایش تاریخ‌ها به صورت شمسی  

---

## ✅ وضعیت فاز اول (تکمیل شده)

موارد زیر در فاز اول پروژه به‌طور کامل پیاده‌سازی شده‌اند:

- تحلیل دامنه و طراحی موجودیت‌ها
- پیاده‌سازی روابط بین جداول با Fluent API
- پیاده‌سازی Onion Architecture (Domain / Application / Infrastructure / Presentation)
- پیاده‌سازی عملیات CRU برای:
  - دسته‌بندی‌ها
  - سرویس‌ها
- افزودن Seed Data اولیه برای جداول پایه
- رعایت اصول Clean Code و Separation of Concerns

---

## 🛠 وضعیت فاز دوم (تکمیل شده)

در فاز دوم، موارد زیر مطابق داکیومنت پروژه پیاده‌سازی شده‌اند:

### 🔐 احراز هویت و نقش‌ها
- پیاده‌سازی **Login و Register** با استفاده از Microsoft Identity
- تفکیک دسترسی کاربران با استفاده از **Roles**
- استفاده از **Areas** برای جداسازی پنل‌ها:
  - Admin
  - Customer
  - Expert

---

### 👤 پروفایل کاربران
- **پروفایل مشتری**
  - مشاهده اطلاعات شخصی
  - ویرایش مشخصات
- **پروفایل کارشناس**
  - مشاهده اطلاعات کامل
  - نمایش مهارت‌ها و خدمات
  - نمایش امتیاز (Average Score)
  - ویرایش اطلاعات پروفایل

---

### 🛡 پنل مدیریت (Admin Panel)
پنل مدیریت شامل بخش‌های زیر است:

- **مدیریت کاربران (CRUD)**
- **مدیریت دسته‌بندی‌ها (CRUD)**
- **مدیریت سرویس‌ها (CRUD)**
- **مدیریت سفارش‌ها**
- **نمایش پیشنهادهای ثبت‌شده برای هر سفارش**
- **مدیریت کامنت‌ها و امتیازات**
  - تایید یا رد کامنت‌ها
  - کنترل امتیازات ثبت‌شده

---

### 📝 سیستم لاگ (Logging)
- پیاده‌سازی لاگ‌گیری در تمامی لایه‌های پروژه
- استفاده از **Serilog**
- اتصال به **Seq** برای ذخیره‌سازی و مشاهده لاگ‌ها
- ثبت لاگ برای:
  - عملیات مهم
  - خطاها
  - رویدادهای سیستمی

---

## 📌 الزامات فنی رعایت شده

- **Soft Delete:**  
  حذف اطلاعات به‌صورت منطقی انجام شده و داده‌ها از دیتابیس فیزیکی حذف نمی‌شوند.

- **Seed Data:**  
  داده‌های اولیه برای:
  - کاربران
  - دسته‌بندی‌ها
  - سرویس‌ها
  - سفارش‌ها
  - پیشنهادها
  - کامنت‌ها  
  در سیستم وجود دارد.

- **مدیریت تصاویر:**  
  تصاویر کاربران روی دیسک ذخیره شده و فقط مسیر تصویر در دیتابیس نگهداری می‌شود.

- **عدم استفاده از Generic Repository:**  
  طبق قوانین پروژه، Repositoryها به‌صورت اختصاصی پیاده‌سازی شده‌اند.

- **تاریخ شمسی:**  
  تمامی تاریخ‌ها در UI به‌صورت شمسی نمایش داده می‌شوند.

---

## 🚀 نتیجه‌گیری

این پروژه مطابق با الزامات فاز اول و دوم پروژه پایانی مکتب ۱۳۵ پیاده‌سازی شده و تمامی موارد خواسته‌شده در داکیومنت پروژه رعایت شده است.  
ساختار پروژه به‌گونه‌ای طراحی شده که قابلیت توسعه و نگهداری در آینده را داشته باشد.

---

👨‍💻 **دانشجو:**  
پروژه پایانی مکتب ۱۳۵
