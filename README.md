# سیستم مدیریت خدمات منزل (Home Service System) 🏠

این پروژه یک پلتفرم جامع برای اتصال مشتریان به متخصصین خدمات منزل است که در قالب پروژه پایانی بوت‌کمپ مکتب شریف (مکتب ۱۳۵) پیاده‌سازی شده است. این سیستم امکان ثبت درخواست توسط مشتریان، ارائه پیشنهاد توسط متخصصین و مدیریت کامل سیستم توسط مدیر (Admin) را فراهم می‌کند.

## 🚀 تکنولوژی‌ها و ابزارها

- **Framework:** ASP.NET Core  
- **Architecture:** Onion Architecture (معماری پیازی)  
- **ORM:** Entity Framework Core (Fluent API)  
- **Identity:** Microsoft Identity  
- **Logging:** Serilog & Seq  
- **UI:** Razor Pages / MVC + Bootstrap  

## 🏗 وضعیت پیشرفت پروژه

این پروژه طبق برنامه‌ریزی کلی شامل **۴ فاز** است که در حال حاضر فازهای اول و دوم به همراه الحاقیه نهایی آن با موفقیت تکمیل شده است.

### ✅ فاز اول: تحلیل، طراحی و زیرساخت

تمرکز این فاز بر پیاده‌سازی هسته اصلی و لایه دسترسی به داده‌ها بود:

- **معماری پیاز:**  
  تفکیک کامل لایه‌ها شامل Core، Infrastructure، Application و Endpoints
- **طراحی دیتابیس:**  
  شناسایی و ایجاد تمامی موجودیت‌ها و روابط بین آن‌ها
- **تنظیمات EF Core:**  
  پیکربندی موجودیت‌ها الزاماً از طریق **Fluent API**
- **Seed Data:**  
  درج داده‌های اولیه برای کاربران، دسته‌بندی‌ها و سرویس‌ها
- **Repositories:**  
  پیاده‌سازی عملیات CRUD پایه بدون استفاده از Generic Repository و Unit of Work

### ✅ فاز دوم: امنیت، پنل مدیریت و لاگینگ

در این فاز امنیت سیستم و قابلیت‌های مدیریتی تکمیل شد:

- **احراز هویت:**  
  پیاده‌سازی سیستم Login و Register با استفاده از **Microsoft Identity**
- **تفکیک دسترسی:**  
  استفاده از **Area**های مجزا برای نقش‌های Admin، Customer و Expert
- **پنل ادمین:**  
  شامل مدیریت کاربران (CRUD)، مدیریت سرویس‌ها و دسته‌بندی‌ها، و نمایش پیشنهادهای هر سفارش
- **مدیریت نظرات:**  
  قابلیت تأیید یا رد کردن کامنت‌ها و امتیازات توسط مدیر
- **پروفایل کاربری:**  
  امکان مشاهده و ویرایش مشخصات و تغییر رمز عبور برای مشتری و کارشناس
- **لاگینگ:**  
  ثبت وقایع در تمامی لایه‌ها و ذخیره‌سازی در **Seq**

### ✅ الحاقیه فاز دوم: فرآیند مشتری

- **مشاهده خدمات:**  
  امکان مشاهده لیست دسته‌بندی‌ها و سرویس‌ها توسط مشتری
- **ثبت درخواست:**  
  انتخاب سرویس موردنظر و ثبت درخواست برای انجام خدمات توسط مشتری

## 🛠 نحوه اجرا (Getting Started)

1. **تنظیمات دیتابیس:**  
   رشته اتصال (ConnectionString) را در فایل `appsettings.json` تنظیم کنید.
2. **لاگینگ:**  
   ابزار **Seq** را برای مشاهده لاگ‌ها اجرا کنید.
3. **بروزرسانی دیتابیس:**  
   در Package Manager Console دستور زیر را اجرا کنید:
   ```powershell
   Update-Database
